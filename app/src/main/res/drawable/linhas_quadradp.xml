<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo do quadrado -->
    <solid android:color="@color/white" />

    <!-- Cor das linhas -->
    <stroke
        android:width="1dp"
    android:color="@color/grey3" /> <!-- Cor das linhas -->

    <!-- Tamanho do quadrado -->
    <size
        android:width="100dp"
    android:height="100dp" />

    <padding
        android:left="2dp"
    android:top="2dp"
    android:right="2dp"
    android:bottom="2dp" /> <!-- Espaçamento inferior -->

</shape>